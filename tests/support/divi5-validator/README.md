# Divi 5 Deterministic Validator (vendored copy)

Copied from the sibling project `Divi 5 Deterministic Validator/src/` (same author).
Used only by the test suite as an output gate: every converted layout must be a
valid Divi 5 block document. Refresh by copying `src/*.php` over this directory.
